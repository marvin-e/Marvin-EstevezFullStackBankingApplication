# bad_bank_capstone

<p> Download all files in github directory </p> 
<p>cd into main folder directory and type "npm install"</p>
<p>Once completed type "npm start"</p>
<p>If app doesn't open automatically navigate to http://localhost:3000</p>
